# 复述训练场 · 第一版开发任务书

> 给任何 AI coding agent 的任务书。按此文档实现即可，实现过程中遇到模糊处，以"最小可用"为准，不要自行加功能。

## 1. 产品一句话

一个纯自用的本地网页应用：每天一张高质量内容卡 → 用户开口复述 → AI 当"听众"追问 → 三行收尾报告（含词库沉淀）。目标是养成每天 10 分钟的口头输出习惯。

## 2. 已敲定的产品决策（不要改）

| # | 决策项 | 结论 |
|---|--------|------|
| 1 | 产品形态 | 纯自用，无账号体系，数据存本地 SQLite |
| 2 | 首版范围 | 只做复述模块 |
| 3 | 练习内容 | 手动添加卡片 + AI 按主题生成卡片（自动抓取管道不做） |
| 4 | AI 角色 | 对话式"听众"：不评分，只追问漏掉的要点，直到说清楚（最多 6 轮） |
| 5 | 交互方式 | 语音输入（浏览器转写）+ 文字追问；不做语音合成 |
| 6 | 设备 | 电脑为主，先不做移动端适配 |
| 7 | 坚持机制 | 页面内"今天练了吗"提示 + 连续天数 + 练习历史（系统通知不做） |
| 8 | 词库 | 收尾报告给 2-3 条用词反馈，替换词自动入库 |
| 9 | 录音 | 只存转写文字，不存音频文件 |

## 3. 技术栈

- **运行时**：Node.js ≥ 22.5（本项目按 Node 24 编写，用内置 `node:sqlite`，无需数据库依赖）
- **后端**：Express，唯一 npm 依赖
- **数据库**：`node:sqlite`（内置），文件 `data/trainer.db`
- **前端**：原生 HTML + CSS + JS 单页（hash 路由，5 个视图），无构建步骤
- **LLM**：OpenAI 兼容 chat/completions API，配置走环境变量（`.env`，通过 `node --env-file=.env` 启动）
- **语音**：浏览器 Web Speech API（Chrome `webkitSpeechRecognition`，`lang='zh-CN'`）

## 4. 目录结构

```
retell-trainer/
├── package.json
├── .env.example          # LLM 配置模板
├── README.md             # 启动说明
├── server.js             # Express 入口 + 全部路由
├── db.js                 # node:sqlite 初始化 + 建表 + 查询函数
├── llm.js                # OpenAI 兼容 API 调用封装
├── prompts.js            # 全部提示词（调优只改这里）
└── public/
    ├── index.html
    ├── style.css
    └── app.js
```

## 5. 数据模型（SQLite）

```sql
CREATE TABLE IF NOT EXISTS cards (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  content TEXT NOT NULL,          -- 500-800 字练习材料
  source TEXT DEFAULT '',         -- 来源备注，可空
  created_at TEXT DEFAULT (datetime('now','localtime')),
  used_at TEXT                    -- 最近一次练习时间，可空
);

CREATE TABLE IF NOT EXISTS sessions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  card_id INTEGER,
  started_at TEXT DEFAULT (datetime('now','localtime')),
  ended_at TEXT,
  turns TEXT DEFAULT '[]',        -- JSON 数组：[{role:'user'|'assistant', text}]
  report TEXT                     -- JSON 收尾报告
);

CREATE TABLE IF NOT EXISTS words (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  word TEXT NOT NULL,             -- 用户原话里的平淡/重复表达
  better TEXT NOT NULL,           -- 建议替换词
  context TEXT DEFAULT '',        -- 卡片标题或一句话上下文
  created_at TEXT DEFAULT (datetime('now','localtime'))
);
```

**连续天数（streak）计算规则**：对 `sessions` 按 `date(started_at)` 去重得到练习日期集合，从今天（若今天未练则从昨天）开始向前数连续天数。实现成一个函数，前端 `GET /api/state` 直接拿结果。

## 6. API 规格

### `GET /api/state`
返回：`{ todayCard, practicedToday, streak, totalSessions }`
- `todayCard`：一张未在今天练过的卡片（优先 `used_at` 最早的），没有则 `null`
- `practicedToday`：布尔

### `GET /api/cards`
返回卡片列表（不含 content，列表页用）：`[{id, title, source, created_at, used_at}]`

### `POST /api/cards`
body：`{ title, content, source? }` → 新建卡片，返回 `{id}`

### `POST /api/cards/generate`
body：`{ topic }` → 调 LLM 生成一张 500-800 字卡片，标题为 `【AI】{topic}`，返回新卡片 `{id, title, content}`。LLM 失败时返回 502 + 错误信息。

### `POST /api/sessions`
body：`{ cardId }` → 新建练习会话，把该卡 `used_at` 更新为当前时间。返回 `{sessionId}`。

### `POST /api/sessions/:id/turn`
body：`{ text }` → 核心接口。
流程：
1. 加载会话与卡片，把 `text` 追加为 `{role:'user'}` 回合
2. 调 LLM（追问提示词，prompts.js 的 `audienceTurnSystem`）：判断复述覆盖了哪些要点、是否还有遗漏
3. **若用户回合数已达 6 轮**，强制进入收尾
4. 返回：
   - `{ type: 'question', question: '…', turn: 3 }`（还要继续追问）
   - `{ type: 'report', report: { summary, words: [{original, better, reason}] } }`（练习结束：调收尾提示词生成报告 → words 写入词库表 → 更新 `ended_at`、`report`、`turns`）

### `GET /api/history`
返回最近 30 条会话：`[{id, date, cardTitle, turnsCount, summary}]`

### `GET /api/words`
返回全部词库：`[{id, word, better, context, created_at}]`

## 7. 练习流程（前端交互）

1. 首页显示今日卡片（标题+全文），下方"开始练习"按钮
2. 练习页：卡片内容 → "开始复述"按钮 → 浏览器录音转写（实时文本出现在可编辑文本框）→ "讲完了"提交
3. 对话区显示 AI 追问（文字气泡）
4. 用户再次开口或打字回复 → 提交 → 循环
5. 收到 `type:'report'` → 展示收尾报告（summary + 词条列表）→ 自动回到首页（streak 更新）
6. 卡片库页：列表 + 手动添加表单 + "AI 按主题生成"输入框
7. 词库页：词条列表（word / better / context）
8. 历史页：会话列表，点击可看 turns 对话回放

**转写修正**：录音转写是实时进文本框的，用户提交前可手动改文本（这是设计的一部分，不是 bug）。

## 8. 提示词初稿（prompts.js）

> 三个提示词都要求"只输出 JSON"。LLM 调用时对 JSON 提示词传 `response_format: {type:'json_object'}`（DeepSeek 支持）；生成卡片提示词不需要。

### 8.1 追问（`audienceTurnSystem`）

```
你是一个认真听人复述的听众，正在听对方复述一篇材料。材料如下：

<材料>
{{content}}
</材料>

你的任务：
1. 先在心里提取这篇材料的 4-6 个关键要点。
2. 对照对方的复述，判断他讲清楚了哪些要点、漏了或讲错了哪些。
3. 输出 JSON，二选一：
   - 还有要点没讲清楚时：{"done": false, "question": "一句自然的追问"}
   - 要点都讲清楚时：{"done": true}

要求：
- question 只追问最关键的、漏掉的那一个点，像真人听众那样自然，不要罗列清单、不要出现"要点1""第二个要点"这类编号。
- 对方如果明显理解偏差，也可以追问确认，而不是直接判错。
- 只输出 JSON，不要输出任何其他文字。
```

### 8.2 收尾报告（`reportSystem`）

```
你是刚才那位听众，练习结束。材料如下：

<材料>
{{content}}
</材料>

对方的完整复述与补充如下：
{{history}}

输出 JSON 收尾报告：
{"summary": "一到两句话：他讲清楚了什么、漏了什么", "words": [{"original": "他原话里用词平淡或重复的表达", "better": "建议的替换词或更生动的说法", "reason": "一句理由"}]}

要求：
- words 给 2-3 条，必须是他这次复述里真实出现过的表达。
- 只输出 JSON。
```

### 8.3 生成卡片（`generateCardSystem`）

```
你是一位内容编辑。围绕主题「{{topic}}」创作一篇 500-800 字的练习材料：
- 可以是小故事、观点论述、人物轶事，适合口头复述练习
- 结构清晰，包含 4-6 个明显可提取的要点
- 语言生动，有值得学习的用词
只输出正文，不要输出标题或任何多余内容。
```

## 9. 验收标准

1. `npm install && node --env-file=.env server.js` 启动，浏览器打开 `http://localhost:3025` 无报错
2. 手动添加一张卡 → 首页能看到 → 开始练习 → 语音转写可用（无麦克风时至少能打字走通）→ AI 追问 → 收尾报告出现
3. 词库出现新词条；历史页出现记录；连续天数正确
4. 未配置 `.env` 时服务能启动，练习中 LLM 调用失败时前端给出清晰报错（"请检查 LLM 配置"），不白屏

## 10. 明确不做（V1.1 再排）

- 自动抓取管道、PWA/系统通知、词库闪卡、移动端适配、录音文件存储、评分体系
